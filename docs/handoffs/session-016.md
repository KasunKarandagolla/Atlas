# ATLAS V2 Session 016 — S2, CandidateSet and deterministic shadow selection

## Checkpoint and authority

- Starting branch: `impl/session-015-v2-phase2a-instrument-identity-fix`.
- Starting and verified remote SHA: `110518b4989f1d672d04e1b3431f3a142734e3ff`.
- Final branch: `impl/session-016-v2-s2-candidateset-selection`.
- Final SHA: recorded in the Session-016 closeout after push; a commit cannot contain its own SHA.
- A clean dedicated worktree at `/home/kasun/Music/atlas-session-016` was created from the exact Session-015 SHA. The older checkout and its untracked supplied freeze and archive were left untouched.
- Authorities inspected: `ATLAS_FINAL_IMPLEMENTATION_CLARIFICATION_AND_V1_FREEZE_COMPLETED.md`; supplied `ATLAS_V2_FINAL_INTRADAY_INTELLIGENCE_IMPLEMENTATION_FREEZE.md` (SHA-256 `bae3e1a9e48aec64d1292e5bc791c2e87949ed33f4124b1f9807b589cc07a484`); `docs/v2/V1_EXIT_AUDIT.md`; `docs/v2/V1_GOLDEN_BASELINE.json`; Session-013, Session-014 and Session-015 handoffs; V2 contracts, instruments, feature/math, S1, memory, data and tests.

## S2 shadow policy

- **IMPLEMENTED / TESTED:** `S2_COMPRESSION_BREAKOUT`, version `1.0.0-shadow`, capital status `SHADOW_ONLY`, policy hash `fbcdacd8ec6a79ea2595fa367d220b55b1d84c326ec3a28d787d23f356062dcd`.
- `S2_EMPIRICAL_QUANTILE_V1` uses sorted finite observations and nearest rank `ceil(p*n)`. The 20th percentile uses `p=0.20`. ATR median and volume median are ordinary empirical medians. Bollinger width is four population standard deviations of 20 closes divided by their arithmetic mean, matching `TECHNICAL_V1`; ATR14 is Wilder smoothing seeded by the first 14 true ranges.
- At trigger bar `t`, the tested width and ATR measurement is completed `t-1`. Exactly 2,880 comparison measurements end at `t-2`; 20-bar width warmup precedes the first comparison observation. Thus exactly 2,901 contiguous confirmed 15M bars, including the trigger, are needed. One fewer is `NOT_ESTIMABLE`. The trigger bar enters no compression, ATR, range or volume statistic.
- The exact preceding 20 bar refs, frozen range high/low, `t-1` ATR ref/value, 2,880 comparison refs, latest width, percentile threshold, ATR median, 1H/4H refs and feature/universe refs are indexed in `S2SetupEvidenceV1`. The latest confirmed 1H and 4H bars are required only as context; no directional trend or EMA gate is added. Raw bars bind `instrument_revision == key.contract_revision`; strategy, BBO, feature and universe use full `InstrumentKeyV2` equality.
- Long requires trigger close strictly above range high plus `0.1 * pre-trigger ATR`; short strictly below range low minus that amount. Equality is `NO_CANDIDATE`. Trigger volume must be strictly above the preceding 20-bar median. Range width from `0.5 ATR` through `3 ATR`, inclusive, is allowed. Stop is the opposite frozen range edge.
- A fresh exact-key executable BBO is required within the versioned five-second envelope. `S2TriggerEvidenceV1` indexes bid, ask, absolute spread, spread bps using midpoint denominator, observed/available time, source ref, threshold, direction and trigger bar ref. There is no S2-specific spread threshold. Stale/invalid BBO is `NOT_ESTIMABLE`.
- The shadow entry interpretation inherits IOC, an adverse 5 bps collar from executable ask for long or bid for short, and no same-epoch repricing from the V2 design source basis. It is encoded in the S2 policy hash; it is **UNVERIFIED** for venue qualification. Candidate deadline is five seconds from confirmed decision time, horizon is two hours, cost ref is `S2_SHADOW_COST_UNESTIMATED_V1`, and quantity is `None`.
- The pure failed-break helper examines only the first two subsequent *closed* 15M bars at the exact next two close boundaries. Inside means `range_low <= close <= range_high`. It uses the original setup ref and frozen range, returns the first matching bar ref, and has no order interface. The policy also fixes the stop, a two-hour time exit, no pyramiding, no stop widening and no same-epoch repricing.
- **IMPLEMENTED / TESTED:** distinct immutable experiment identities: raw range `df60e1aa296b478c66f8fb5d42ece2c00a06a876c67b9b254922aa1c1e58986d`; compression only `8685e38883484aee6256bf314a0dd2fefdc8be117ffaa42f191d513f994debdd`; volume only `fef2fe6e112ab0475d1faf3da6c7adf20ea97c33357f88cea016b0b5776dc5c4`; compression plus volume baseline `fbcdacd8ec6a79ea2595fa367d220b55b1d84c326ec3a28d787d23f356062dcd`; delayed/retest `4466f61a679f7a2da64431d19c386f310c9d812f623820a481cf5b7464d103a4`. These are research identities; only the baseline coordinator generates candidates here. Optional L2/OFI does not alter the baseline hash. Variant economic comparison is **NOT ESTIMABLE**.

## CandidateSet and research acceptance

- **IMPLEMENTED / TESTED:** selection policy `S1_S2_SCANNER_RANK_V1`, hash `36f8fd58c9e791ea580a1855f9e98555131e0828604d484eda3df4c7d5529cac`. Its complete rank key is scanner rank ascending, policy ID ascending, canonical `InstrumentKeyV2` JSON ascending, candidate ID ascending. Scanner evidence has candidate ID, rank, scanner policy/version, universe ref, decision event, source artifact ref and availability time; it is indexed immutably. The policy records a five-second scanner evidence age limit. No outcome, model confidence or economic score enters ranking.
- Candidate actions must already be indexed with exact wire content and matching supplied policy hash, full universe key, decision cutoff and unexpired deadline. Unknown, duplicate, conflicting, wrong-policy, wrong-universe and wrong-event candidates fail closed before set creation.
- `CandidateSetV2` is indexed for every decision event, including empty ones. All generated candidate entries remain, sorted by candidate ID as required by the frozen wire contract. Explicit `rank` fields carry selection order. A complete eligible set selects rank 1. All known rejections, or an empty event, produce `NO_CANDIDATE`. Missing, stale, contradictory or future scanner evidence produces `NOT_ESTIMABLE` with every generated entry and reason retained. Selection itself does not size or approve capital.
- Set identity binds decision event ID, universe ref, selection policy hash, every candidate ref, every supplied selection evidence ref, cutoff and producer version. Input candidate order does not affect ranks, selected ID or content hash. An immutable decision-event index rejects a different set for the same event/universe/policy, so a later scanner revision cannot replace the earlier set.
- After durable set indexing, `ResearchCandidateAcceptanceV1` is indexed for each incorporated candidate. It binds candidate ref, set ref, event, feature hash, selection status, selected state, pipeline version and acceptance time. S1's existing exact watch/candidate/feature acceptance checks remain. In the integrated fixture S2 ranks first, S1 remains in the set and its confirmed watch reaches `HANDED_OFF` as an unselected pipeline-accepted candidate. No TradePlan, evaluation, order, reservation or approval is created.

## Verification and remaining gates

- Focused Session-016 tests: **TESTED**, 12 passed. They cover exact 30-day support and one-bar shortage, causal exclusions and future-tail replay, strict thresholds and inclusive width limits, long/short, missing context/BBO/revision, failed break, variant identity, candidate ranking/ties/shuffle, empty and rejected events, unindexed/mismatched candidates, stale/future evidence, and the bounded S1/S2 handoff.
- All `tests/v2`: **TESTED**, 90 passed on the final policy identity.
- Required V1 invariant smoke: **TESTED**, 11 passed. S1 policy hash remains `c559659ace0239200f7d26d81a24b489a8a4ee0bc849b6954faf901126b5dff0`.
- Ruff, mypy (`src/atlas/v2 tests/v2`), compileall and `git diff --check`: **TESTED**. No V1 production file, frozen V1 golden, persistence schema, dependency or lock file changed.
- V1 golden SHA-256: **TESTED**, `b6d47c1c7a2e416304dd57d9055599201c474c8bada600cf23c2cbc09f90e53c`. Dependency lock SHA-256: **TESTED**, `0d7b5cc6129aab127f07a1b5bce4b7794eec5c48db0f99451eba675031ca2b39`.
- Environment: Python 3.12.13, Linux `5.15.0-177-generic` x86_64, locked virtual environment from the invoking checkout. No credential or authenticated venue call was used.
- Secret scan: **TESTED** using tracked-file and staged-diff fallback because `gitleaks`, `trufflehog` and `detect-secrets` executables were unavailable. No high-confidence secret, private key or non-template `.env` was found.
- S2 and selection economic value: **NOT ESTIMABLE**. Deterministic risk quantity, action sizing, scenarios, economic evaluator, M0, rolling risk, desktop and matured outcomes remain **TEST GATE**. V2 Phase 2 overall remains **TEST GATE**; no capital authority is enabled. Tier 4 remains false, model influence zero and `assisted_enabled=false`.
- 72-hour public soak: **BLOCKED BY ENVIRONMENT**. Exact Chronos-2 checkpoint/package and Windows worker isolation: **UNVERIFIED**. Six V1 Bybit capabilities remain **UNVERIFIED / TEST GATE**.
